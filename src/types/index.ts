/**
 * Type Definitions - Central export for all application types.
 *
 * This barrel file re-exports all TypeScript interfaces and types
 * used throughout the Babble application, providing a single import point.
 *
 * @module types
 *
 * @example
 * ```typescript
 * import type { Document, DocumentMeta, Comment } from '@/types';
 * ```
 */

export * from './document';
export * from './comment';
